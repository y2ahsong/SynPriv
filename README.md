# SynPriv
소량 데이터 환경에서 합성 데이터의 프라이버시 측정

## 환경 설정


```bash
conda create -n clava python=3.10 -y
conda activate clava
pip install -r requirements_clava.txt
```
```bash
conda create -n rctgan python=3.9
conda activate rctgan
pip install -r requirements_rctgan.txt
```
```bash
conda create -n rtf python=3.9 -y
conda activate rtf
pip install -r requirements_rtf.txt
```
```bash
conda create -n tabula python=3.9 -y
conda activate tabula
pip install -r requirements_tabula.txt
```